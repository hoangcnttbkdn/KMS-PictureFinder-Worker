import Queue, { Job, DoneCallback } from 'bull'
// import { queueConfig, WORKER_NAME } from '../..'

// const handleQueue = new Queue(WORKER_NAME, queueConfig)

// handleQueue.process((job: Job, done: DoneCallback) => {
//   job.data
// })
